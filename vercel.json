{
  "buildCommand": "cd artifacts/jane-doe-realty && npx vite build --config vite.config.production.ts",
  "outputDirectory": "artifacts/jane-doe-realty/dist",
  "installCommand": "npm install -g pnpm && pnpm install --no-frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
